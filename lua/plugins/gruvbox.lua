-- This plugin is for the gruvbox colorscheme
-- config function sets the colorscheme.

return {
	"ellisonleao/gruvbox.nvim",
	priority = 1000,
	opts = {},
	--config = function()
	--	vim.cmd.colorscheme("gruvbox")
	--end,
}
